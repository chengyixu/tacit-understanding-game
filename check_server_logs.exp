#!/usr/bin/expect -f

spawn ssh root@47.117.176.214
expect "password:"
send "0212Connect!\r"
expect "~]#"

send "cd /moqiyouxi_backend\r"
expect "]#"

send "tail -50 server.log | grep -E 'join|Join|room|Room|ERROR|error'\r"
expect "]#"

send "exit\r"
expect eof
