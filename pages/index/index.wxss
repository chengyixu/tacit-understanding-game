.title {
  font-size: 48rpx;
  font-weight: 300;
  margin-bottom: 60rpx;
  letter-spacing: 6rpx;
}

.version-info {
  position: fixed;
  bottom: 50rpx;
  left: 50%;
  transform: translateX(-50%);
  font-size: 22rpx;
  color: #B8B1A6;
  opacity: 0.8;
  letter-spacing: 1rpx;
}