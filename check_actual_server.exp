#!/usr/bin/expect -f

spawn ssh root@47.117.176.214
expect "password:"
send "0212Connect!\r"
expect "~]#"

send "cd /moqiyouxi_backend\r"
expect "]#"

send "grep -n 'elif action ==' server0405.py | head -10\r"
expect "]#"

send "grep 'handle_join_room\\|handle_create_room' server0405.py | head -5\r"
expect "]#"

send "exit\r"
expect eof
