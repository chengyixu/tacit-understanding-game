#!/usr/bin/expect -f

set timeout 10
spawn ssh root@43.137.34.201 "cd /moqiyouxi_backend && tail -20 server.log"
expect {
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "0212Connect!\r"
    }
}
expect eof