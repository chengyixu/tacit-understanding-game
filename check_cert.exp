#!/usr/bin/expect -f

set timeout 10
spawn ssh root@43.137.34.201 "cd /moqiyouxi_backend && openssl x509 -in aiconnector.cn_bundle.pem -noout -dates"
expect {
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "0212Connect!\r"
    }
}
expect eof