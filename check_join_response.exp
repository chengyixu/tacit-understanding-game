#!/usr/bin/expect -f

spawn ssh root@47.117.176.214
expect "password:"
send "0212Connect!\r"
expect "~]#"

send "cd /moqiyouxi_backend\r"
expect "]#"

send "grep -A 20 'def handle_join_room' server0405.py | grep 'action.*:.*joined'\r"
expect "]#"

send "exit\r"
expect eof
